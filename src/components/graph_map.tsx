import { useState } from "react";
import Draggable from "react-draggable";
import { Vertex } from "./vertex";


export default function GraphMap() {
    const [vertices, setVertices] = useState([]);
    const [v_count, setVCount] = useState(0);

    return (

    );
}