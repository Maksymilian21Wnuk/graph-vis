


export enum Weight {
    WEIGHTED = "1",
    UNWEIGHTED = "",
};

export enum NodeColor {
    VISITED = "yellow",
    UNVISITED = "white",
    CURRENT = "red",
};

export enum Value {
    NOT_SELECTED = -1,
}